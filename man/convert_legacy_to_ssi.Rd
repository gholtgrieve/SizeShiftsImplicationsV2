% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_legacy.R
\name{convert_legacy_to_ssi}
\alias{convert_legacy_to_ssi}
\title{Convert a legacy Ohlberger .RData environment to a package-compliant \code{ssi_run} object.}
\usage{
convert_legacy_to_ssi(rdata_file, env_yaml = NULL)
}
\arguments{
\item{rdata_file}{Character. Path to the legacy \code{.RData} file containing
the saved environment from a run (must include objects like \code{data.list},
\code{obs.list}, \code{egg.list}, \code{fec.list}, \code{sr_sim.list}, \code{parms.list}, etc.).}

\item{env_yaml}{Optional. Path to a YAML file with environment metadata
(e.g. RNG kind, BLAS threads, pkg version) to enrich \code{meta} and \code{parameters}.}
}
\value{
A list of class \code{ssi_run} with the following top-level components:
\itemize{
\item \code{meta}       : list with run-level metadata (timestamp, niter, nscen, etc.)
\item \code{scenarios}  : tibble of scenario metadata (with \code{scen_num}, no \code{No})
\item \code{results}    : list with sublists
(\code{para.list}, \code{sr_sim.list}, \code{fec.list}, \code{egg.list}, \code{S_msy.list},
\code{data.list}, \code{obs.list}, \code{ret_age.list}, \code{meanSaA.list},
\code{propfemale.list}, \code{select_age.list}, \code{MSY_Goals}, \code{impl_errors.list})
\item \code{parameters} : list of run parameters (ny, nyh, nyi, goalfreq, firstrev,
review_years, seednum, …)
}
}
\description{
This function loads a saved legacy \code{.RData} file (the whole run environment)
and converts it into a strict \code{ssi_run} object with the correct top-level
keys: \strong{meta}, \strong{scenarios}, \strong{results}, and \strong{parameters}.
}
\details{
The sublists under \verb{$results} are direct copies of the legacy objects,
with only the scenario level renamed to \verb{scen_1, scen_2, …}.
}
