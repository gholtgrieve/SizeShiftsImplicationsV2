% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summarize_50_year_avg.R
\name{summarize_50_year_avg}
\alias{summarize_50_year_avg}
\title{Summarize 50-year average metrics from model data}
\usage{
summarize_50_year_avg(data, years = 50L)
}
\arguments{
\item{data}{\code{ssi_run} object, file path to a saved run \code{.rds}, or legacy list.}

\item{years}{Integer. If using an \code{ssi_run}, number of most recent years to
summarize (default 50). Ignored if legacy \code{year_index} is supplied.}
}
\value{
A named list with elements \code{escapement}, \code{harvest}, and \code{return}.
Each is a list (one element per scenario) containing:
\itemize{
\item \code{means}: numeric vector (length = niter) of iteration means,
\item \code{sd}: numeric vector (length = niter) of iteration SDs,
\item \code{quantiles}: matrix niter x 9 with iteration-wise quantiles.
}
}
\description{
Accepts either:
\itemize{
\item an \code{ssi_run} object returned by \code{run_scenarios()},
\item a character path to an \code{.rds} saved by \code{run_scenarios()}, or
\item a legacy list with elements: \code{obs_list}, \code{iter_names}, \code{year_index}.
}
}
\details{
The function computes, for each scenario and iteration, the mean across the
last 50 years (or supplied indices) for \code{obsEsc}, \code{obsHarv}, and \code{obsRet},
then returns iteration-level summaries (means, SDs, quantiles) grouped by
scenario, preserving the original return structure.
}
