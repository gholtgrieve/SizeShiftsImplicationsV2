% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_Ohlberger_figure_2.R
\name{.make_Ohlberger_figure_2}
\alias{.make_Ohlberger_figure_2}
\title{Generate Ohlberger Figure 2: \eqn{S_{MSY}} at first post-historical review}
\usage{
.make_Ohlberger_figure_2(
  data,
  output_dir = tempdir(),
  file_basename = "Figure2",
  width_in = 6,
  height_in = 4
)
}
\arguments{
\item{data}{\code{ssi_run} object from \code{\link[=run_scenarios]{run_scenarios()}}.}

\item{output_dir}{Directory to save the figure (default: temp directory).}

\item{file_basename}{Base name for the output folder/file (default: "Figure2").}

\item{width_in, height_in}{Dimensions (inches) for the saved PDF.}
}
\value{
Invisibly returns a list with:
\itemize{
\item \code{plot}: the ggplot object
\item \code{data}: tidy plotting data (one value per iteration)
\item \code{outdir}: output directory used
}
}
\description{
Reproduces the published Figure 2. For each scenario, we extract the
\eqn{S_{MSY}} value at the \strong{first review at or after} the end of the
historical period (\eqn{nyi + nyh}), then summarize \strong{across iterations}
within each (trends × selectivity × mgmt) cell. The plot shows medians
(points) with central \strong{50\%} and \strong{80\%} intervals (vertical bars).
Columns = gear selectivity; x = demographic trend scenarios; color =
management method (TRM/YPR/DLM).
}
\section{Data source}{

\itemize{
\item Input is an \strong{\code{ssi_run}} from \code{\link[=run_scenarios]{run_scenarios()}}.
\item Required: ssi_run$parameters must include nyi, nyh, ny, goalfreq,
firstrev, review_years. The function validates these and errors if missing.
\item Uses \code{data$results$S_msy}: a nested list keyed by scenario (e.g.,
\code{"scen_<num>"}), where each element is a list of numeric vectors
(one value per \strong{review year}) for each iteration.
}
}

\section{Review year selection}{

Review years follow the simulation setup:
\code{seq(nyi + firstrev, ny, by = goalfreq)}. The figure uses the \strong{first
review at or after} calendar year \code{nyi + nyh} (i.e., the first
post-historical review). Internally this is resolved via
\code{.get_review_years()} and \code{.get_review_index_post_history()}.
}

\section{Scenario filtering & labeling}{

\itemize{
\item Scenario labels and factor orders are standardized via
\code{.standardize_scenario_labels()}.
\item Keep only \code{factorMSY == "MSY"}.
\item Exclude \code{trends == "ASL trends continued"}.
\item Facet by \code{selectivity} with \code{scales = "free_y"}.
}
}

\section{Statistics (across iterations)}{

For each (trends × selectivity × mgmt) cell, compute the \strong{median} and the
central \strong{50\%} (\eqn{0.25, 0.75}) and \strong{80\%} (\eqn{0.10, 0.90}) intervals
of the per-iteration \eqn{S_{MSY}} values at the selected review.
}

\section{Plot design}{

\itemize{
\item x: \code{trends} (in order); y: \eqn{S_{MSY}} (native units; no scaling).
\item color: \code{mgmt} with palette \code{c("darkgray","deepskyblue3","orange")}.
\item Two \code{stat_summary()} layers: 80\% band (thin) and 50\% band (thicker),
both centered on the median.
\item y label: \eqn{S_{MSY}} rendered via \code{expression(""*S[MSY]*"")}.
}
}

\section{Files written}{

\itemize{
\item \code{file.path(output_dir, "Figure2", "Figure2.pdf")}
\item \code{file.path(output_dir, "Figure2", "Figure2.csv")}
}
}

\references{
Ohlberger, J., Schindler, D. E., & Staton, B. A. (2025). Accounting for
salmon body size declines in fishery management can reduce conservation
risks. \emph{Fish and Fisheries}, 26, 113–130. https://doi.org/10.1111/faf.12869
}
\keyword{(internal)}
\keyword{2}
\keyword{Figure}
\keyword{Make}
\keyword{Ohlberger}
\keyword{internal}
