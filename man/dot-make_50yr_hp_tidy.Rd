% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_Kusko_figure_E.R
\name{.make_50yr_hp_tidy}
\alias{.make_50yr_hp_tidy}
\title{Helper: Build tidy HP (Harvest / ProbZeroHarvest) 50-yr summaries with quantiles}
\usage{
.make_50yr_hp_tidy(data, summary_list, scen_df, statistic = "mean")
}
\arguments{
\item{data}{\code{ssi_run} object (from \code{run_scenarios()}).}

\item{summary_list}{Output from \code{.summarize_50_year_avg(data)}.}

\item{scen_df}{Scenario metadata tibble with standardized labels and column \code{scen}
matching the order of scenarios (e.g., "scenario_1", ...).}

\item{statistic}{Point estimate for harvest axis: \code{"mean"} or \code{"median"}.}
}
\value{
Tidy data.frame (one row per scenario) with columns:
\verb{mgmt, selectivity, trends, factorMSY, scen, harv_mean, harv_q10, harv_q25, harv_q50, harv_q75, harv_q90, p0_mean,  p0_q10,  p0_q25,  p0_q50,  p0_q75,  p0_q90}
}
\description{
Harvest stats are read from \code{summary_list$harvest} (output of
\code{.summarize_50_year_avg()}). Probability of zero harvest (P0) is computed
directly from \code{data$results$obs} by:
\enumerate{
\item Identifying the common last-50-year window across iterations (same rule
as \code{.summarize_by_year()}): the minimum available length across all
iterations and scenarios, capped at 50.
\item For each scenario and iteration, computing the 50-yr mean of the
zero-harvest indicator: \code{as.numeric(obsHarv <= 0)}.
\item Across iterations, summarizing to mean and quantiles (10, 25, 50, 75, 90\\%).
}
}
\keyword{internal}
