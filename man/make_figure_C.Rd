% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_figure_C.R
\name{make_figure_C}
\alias{make_figure_C}
\title{Generate Figure C: Time series of escapement, harvest, and probability of zero harvest
by trend, harvest strategy, and management}
\usage{
make_figure_C(
  outputs,
  selectivity_filter = "unselective",
  summary_stats = c(mean = "Mean", ymin = "25\%", ymax = "75\%"),
  colors = c("darkgray", "deepskyblue3", "orange"),
  output_dir = ".",
  file_basename = "FigureC"
)
}
\arguments{
\item{outputs}{\code{ssi_run} (preferred) or legacy outputs list.}

\item{selectivity_filter}{Character; filter for selectivity type (default: "unselective").}

\item{summary_stats}{Named vector mapping plot columns to summary statistic names
(default: c(mean = "Mean", ymin = "25\%", ymax = "75\%")).}

\item{colors}{Character vector of colors (default: c("darkgray", "deepskyblue3", "orange")).}

\item{output_dir}{Directory to save plots and data (default: current working directory).}

\item{file_basename}{Base filename for saved files (default: "FigureC").}
}
\value{
A list with:
\itemize{
\item \code{data}: tidy data.frame used for plotting
\item \code{plots}: named list of ggplot objects by management strategy
}
}
\description{
Accepts:
\itemize{
\item an \code{ssi_run} object from \code{run_scenarios()} (recommended), or
\item legacy flat outputs compatible with older code.
}
}
\details{
Internally calls \code{summarize_by_year()} (which defaults to the last 50 years
for \code{ssi_run} inputs). Produces ribbons for central intervals and overlays a
smoothed (5-year rolling mean) probability of zero harvest on the secondary axis.
}
