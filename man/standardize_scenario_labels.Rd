% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/labels_helper.R
\name{standardize_scenario_labels}
\alias{standardize_scenario_labels}
\title{Standardize scenario labels and factor orders (internal)}
\usage{
standardize_scenario_labels(df)
}
\arguments{
\item{df}{A data.frame/tibble with any of the columns above.}
}
\value{
The same data.frame with standardized character values and ordered factors.
}
\description{
Ensures consistent printed labels across all figures and summaries.
}
\details{
\itemize{
\item \strong{factorMSY}: \code{0.75} → \code{"liberal"}, \code{1} → \code{"MSY"}, \code{1.5} → \code{"precautionary"}
\item \strong{trends}:
\itemize{
\item \code{"no trends"} (and \code{"No trends"}) → \code{"no trends"}
\item \code{"age-sex-length trends"} → \code{"ASL trends stabilized"}
\item \code{"age-length trends"} → \code{"AL trends stabilized"}
\item \code{"continuing trends"} → \code{"ASL trends continued"}
}
\item \strong{mgmt}: map common long-form to short codes: \code{"smsy_goal"} → \code{"TRM"}, \code{"s_eq_goal"} → \code{"YPR"}, \code{"smsy_dlm_goal"} → \code{"DLM"}
\item \strong{selectivity}: standardize to \code{"small-mesh"}, \code{"unselective"}, \code{"large-mesh"}
}

If a column is absent, it is ignored. Factors are (re)leveled for stable facet/legend order:
\itemize{
\item \code{factorMSY}: \code{c("liberal","MSY","precautionary")}
\item \code{trends}:    \code{c("no trends","ASL trends stabilized","AL trends stabilized","ASL trends continued")}
\item \code{mgmt}:      \code{c("TRM","YPR","DLM")} (plus any other levels appended at end, if present)
\item \code{selectivity}: \code{c("small-mesh","unselective","large-mesh")}
}
}
\keyword{internal}
