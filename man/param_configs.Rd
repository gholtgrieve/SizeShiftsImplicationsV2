% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/param_configs.R
\docType{data}
\name{param_configs}
\alias{param_configs}
\title{Built-in parameter profiles (\code{param_configs})}
\format{
A named list with two elements:
\describe{
\item{\code{Ohlberger}}{Baseline profile for the Ohlberger-style setup.}
\item{\code{Kuskokwim}}{Baseline profile for the Kuskokwim setup.}
}

Each profile list can include (non-exhaustive):
\itemize{
\item Core switches: \code{sim_recruits}, \code{est_method}, \code{ricker_type}, \code{var}, \code{optimize}
\item Time horizon: \code{nyi}, \code{nyh}, \code{ny}  (note: \code{futureT} is scenario-driven)
\item Errors: \code{obserr}, \code{hobserr}, \code{harverr}
\item Regimes: \code{reglength}, \code{regstr}
\item Stock–recruit: \code{procerr}, \code{rho}, \code{alpha_mean}, \code{beta_mean}, \code{sr_corr}, \code{sr_parms_sd}
\item Ages/structure: \code{ages}, \code{meanageini}, \code{sdage}
\item Trend baselines (multiplied by scenario switches): \code{agetrend}, \code{sizetrends}, \code{propFtrend}
\item Sex: \code{propF}, \code{agediff}
\item Growth/size: \code{vonB_Linf}, \code{vonB_k}, \code{ocean0s}, \code{sdSaA}
\item Allometry: \code{allometry}
\item Alternative SR params: \code{alt_sr_param}
}
}
\usage{
param_configs
}
\description{
A named list of \strong{built-in parameter profiles} used to configure
\code{run_model()} and higher-level helpers. Each element is a list of
baseline values; \strong{scenario-specific overrides} (e.g., \code{futureT},
\code{harvmgmt}, \code{factorMSY}, \code{maxsel}, \code{sdsel}, and switches for
\code{agetrend}, \code{sizetrends}, \code{propFtrend}) are applied at runtime.
}
\details{
These profiles are intended to be layered over a generic
\code{\link[=default_config]{default_config()}} and then modified with
\code{\link[=apply_scenario_overrides]{apply_scenario_overrides()}}.

These are \strong{baselines} only. For a full, ready-to-run config:
\enumerate{
\item Start with \code{default_config()},
\item Overlay one of \code{param_configs[[...]]},
\item Call \code{apply_scenario_overrides(cfg, scen_row)}, then
\item Set \code{cfg$j}, \code{cfg$k}, and \code{cfg$seednum}.
}
}
\examples{
# Inspect available profiles
names(param_configs)

# Build a config using the Ohlberger profile (pseudo-example):
# cfg <- utils::modifyList(default_config(), param_configs[["Ohlberger"]])
# cfg <- apply_scenario_overrides(cfg, scen_row)  # scen_row = one row from scenarios
# cfg$seednum <- 123; cfg$j <- 1L; cfg$k <- 1L
# run_model(cfg)

# Use directly in a higher-level runner (if it accepts `params =`):
# run_scenarios("all", niter = 10, params = param_configs[["Kuskokwim"]])

}
\seealso{
\code{\link[=default_config]{default_config()}}, \code{\link[=apply_scenario_overrides]{apply_scenario_overrides()}}, \code{\link[=run_scenarios]{run_scenarios()}}, \code{\link[=run_model]{run_model()}}
}
\keyword{datasets}
